#include <bits/stdc++.h>
using namespace std;

int ans;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int input;
    while (cin >> input) ans += input;
    cout << ans;
    return 0;
}