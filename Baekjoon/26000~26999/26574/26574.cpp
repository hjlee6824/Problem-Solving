#include <bits/stdc++.h>
using namespace std;

int n;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;

    while (n--) {
        int input;
        cin >> input;
        cout << input <<  ' ' << input << '\n';
    }

    return 0;
}