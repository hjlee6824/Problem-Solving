#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    while (true)
    {
        int M, F;
        cin >> M >> F;

        if (M + F == 0) break;

        cout << M + F << '\n';
    }

    return 0;
}